# macOS
.DS_Store

# node
node_modules/

# local secrets — nothing in this project needs them, but just in case
.env
.env.local

# enrichment run logs — the data itself is committed, the noise is not
data/enrich-run.log
data/retry-run.log
